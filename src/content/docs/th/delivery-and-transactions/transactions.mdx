---
title: 'Transactions'
description: 'เขียนข้าม partition แบบ atomic ด้วย transactional producer transactionalId วงจรชีวิตของ transaction read_committed และการเสริมความแข็งแรงจาก KIP-890 ใน Kafka 4.x'
sidebar:
  order: 2
---

import Mermaid from '../../../../components/Mermaid.astro';
import Callout from '../../../../components/Callout.astro';
import Quiz from '../../../../components/Quiz.tsx';
import ProgressTracker from '../../../../components/ProgressTracker.tsx';

## ไอเดียในหนึ่งประโยค

**transaction** ของ Kafka ให้ producer เขียนไปหลาย partition (และ commit consumer offset) แบบ **atomic** ทุกอย่างใน transaction ปรากฏพร้อมกันตอน commit หรือไม่ปรากฏเลยตอน abort

## ตั้งค่า transactional producer

transaction ต่อยอดจาก idempotent producer คุณเพิ่ม **`transactionalId`** ซึ่งเป็น id ที่เสถียรและให้ Kafka fence ตัว zombie ของ producer เชิงตรรกะเดียวกันออกไป พร้อมกับ `idempotent: true` และ `maxInFlightRequests: 1` การ connect producer จะลงทะเบียน transactional id ให้เอง — ต่างจาก client ฝั่ง Java ตรงที่ KafkaJS ไม่มี `initTransactions()` แยกต่างหาก

```typescript
import { Kafka } from 'kafkajs'

const kafka = new Kafka({ clientId: 'payments', brokers: ['localhost:9092'] })

// A transactional producer: a stable transactionalId is the key ingredient
const producer = kafka.producer({
  transactionalId: 'payments-tx-1',   // stable id enables transactions + zombie fencing
  idempotent: true,                    // required foundation
  maxInFlightRequests: 1,
})

await producer.connect()               // connecting registers the transactional id
```

## วงจรชีวิตของ transaction

เริ่มหน่วยงานหนึ่งด้วย `producer.transaction()` send ทุกตัวบน `txn` ที่ได้คืนมาถูก buffer เข้า transaction `txn.commit()` ทำให้ทุกตัวปรากฏพร้อมกันแบบ atomic และ `txn.abort()` ทิ้งทั้งหมด

```typescript
const txn = await producer.transaction()   // begin
try {
  await txn.send({ topic: 'accounts', messages: [{ key: debitKey, value: debit }] })
  await txn.send({ topic: 'accounts', messages: [{ key: creditKey, value: credit }] })
  await txn.commit()     // both writes appear together, or neither
} catch (e) {
  await txn.abort()      // roll back the whole unit
}
```

## consumer ต้องอ่านแบบ committed

transaction มีความหมายก็ต่อเมื่อฝั่งอ่านเมิน data ที่ abort และที่ยัง in-flight ตั้ง consumer เป็น **`readUncommitted: false`** — คือ **read_committed** และเป็นค่า default ของ KafkaJS — เพื่อให้มันไม่เห็น record จาก transaction ที่ abort หรือที่ยังเปิดอยู่

```typescript
const consumer = kafka.consumer({
  groupId: 'ledger',
  readUncommitted: false,   // read_committed: skip aborted and in-flight records
})
```

<Mermaid title="transaction commit หลาย write แบบ atomic" code={txnDiagram} />

export const txnDiagram = `flowchart LR
  init["connect() once (registers txn id)"] --> begin["producer.transaction()"]
  begin --> s1["txn.send to accounts-0"]
  begin --> s2["txn.send to accounts-2"]
  s1 --> commit["txn.commit(): all visible together"]
  s2 --> commit
  begin -.->|on error| abort["txn.abort(): nothing visible"]`;

## KIP-890 ใน Kafka 4.x

Kafka 4.x finalize **KIP-890 Transactions Server-Side Defense** ที่เสริมความแข็งแรงให้ transactional protocol รับมือ edge case อย่าง transaction ที่ค้างและ zombie producer พอ finalize การอัปเกรด 4.0 protocol ใหม่ที่แข็งแรงกว่าก็ถูกเปิดใช้ ทำให้ transactional pipeline บน Kafka 4.x ทนทานกว่าเวอร์ชันเก่าโดยที่คุณไม่ต้องแก้ code ฝั่งตัวเอง

<Callout title="สรุปหนึ่งบรรทัด">
**transaction** ทำให้การเขียนข้าม partition เป็น **atomic** ตั้ง `idempotent: true` บวก **`transactionalId`** ที่เสถียร (และ `maxInFlightRequests: 1`) แล้วใช้ `producer.transaction()` → `txn.commit()` (หรือ `txn.abort()`) ฝั่งอ่านต้องใช้ **`readUncommitted: false`** (read_committed) เพื่อข้าม data ที่ abort หรือเปิดค้าง Kafka 4.x เสริมความแข็งแรงทั้งหมดนี้ผ่าน **KIP-890** แบบโปร่งใส
</Callout>

export const quizTransactions = [
  {
    q: 'transaction ของ Kafka รับประกันอะไร',
    options: [
      'เขียนไป partition เดียวเร็วขึ้น',
      'การเขียนหลายตัว (และ commit offset) ปรากฏแบบ atomic ทั้งหมดตอน commit หรือไม่มีเลยตอน abort',
      'บีบอัดอัตโนมัติ',
      'record ไม่เคยถูก retain',
    ],
    answer: 1,
  },
  {
    q: 'บทบาทของ `transactionalId` คืออะไร',
    options: [
      'มันตั้งชื่อ topic',
      'id ที่เสถียรของ producer เชิงตรรกะ ที่ให้ Kafka fence ตัว zombie ออกไป',
      'มันตั้งจำนวน partition',
      'มันคือ consumer group id',
    ],
    answer: 1,
  },
  {
    q: 'consumer ต้องตั้งอะไรเพื่ออ่าน transactional data ให้ถูกต้อง',
    options: [
      '`readUncommitted: true`',
      '`readUncommitted: false` (read_committed) เพื่อข้าม record ที่ abort และที่ยัง in-flight',
      '`autoCommit: true`',
      'fetch size ที่ใหญ่ขึ้น',
    ],
    answer: 1,
  },
  {
    q: 'KIP-890 ทำอะไรใน Kafka 4.x',
    options: [
      'เอา transaction ออก',
      'เสริมความแข็งแรงให้ transactional protocol (server-side defense) รับมือ transaction ค้างและ zombie แบบโปร่งใส',
      'บังคับให้ใช้ ZooKeeper สำหรับ transaction',
      'ปิด idempotence',
    ],
    answer: 1,
  },
];

<Quiz client:visible id="delivery-and-transactions/transactions" questions={quizTransactions} />

<ProgressTracker client:visible id="delivery-and-transactions/transactions" />
