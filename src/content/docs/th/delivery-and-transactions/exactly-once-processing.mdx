---
title: 'Exactly-Once Processing'
description: 'รูปแบบ consume-transform-produce ด้วย transactions ใช้ txn.sendOffsets commit input offset กับ output record แบบ atomic'
sidebar:
  order: 3
---

import Mermaid from '../../../../components/Mermaid.astro';
import Callout from '../../../../components/Callout.astro';
import Quiz from '../../../../components/Quiz.tsx';
import ProgressTracker from '../../../../components/ProgressTracker.tsx';

## ไอเดียในหนึ่งประโยค

use case คลาสสิกของ exactly-once คือ **consume-transform-produce** อ่านจาก input topic แปลง แล้วเขียนไป output topic และ Kafka ทำให้มันเป็น exactly-once ด้วยการ commit **input offset ภายใน transaction เดียวกัน** กับ output record

## ทำไม offset ต้องเข้าไปอยู่ใน transaction

ลองนึกถึงการอ่าน `orders` เติมข้อมูลแต่ละตัว แล้วเขียนไป `orders-enriched` ถ้าคุณ commit output ใน transaction แต่ commit input offset แยกต่างหาก การล่มระหว่างสองจังหวะจะทำให้ประมวลผลซ้ำ (output ซ้ำ) หรือข้าม (input หาย) วิธีแก้คือ commit offset ของ consumer **ให้เป็นส่วนหนึ่งของ transaction ของ producer** ด้วย `txn.sendOffsets()` ทีนี้ output record กับความคืบหน้าของ input จะ commit หรือ abort **พร้อมกัน**

```typescript
const producer = kafka.producer({ transactionalId: 'enricher-1', idempotent: true, maxInFlightRequests: 1 })
const consumer = kafka.consumer({ groupId: 'enricher', readUncommitted: false })
await producer.connect()
await consumer.connect()
await consumer.subscribe({ topic: 'orders' })

await consumer.run({
  autoCommit: false,                               // offsets are committed inside the transaction
  eachBatch: async ({ batch }) => {
    const txn = await producer.transaction()       // begin
    try {
      for (const message of batch.messages) {
        await txn.send({ topic: 'orders-enriched', messages: [{ key: message.key, value: enrich(message.value) }] })
      }
      // Commit the input offsets in the SAME transaction as the output
      await txn.sendOffsets({
        consumerGroupId: 'enricher',
        topics: [{ topic: batch.topic, partitions: [{ partition: batch.partition, offset: (Number(batch.lastOffset()) + 1).toString() }] }],
      })
      await txn.commit()                           // outputs + offsets are atomic
    } catch (e) {
      await txn.abort()
    }
  },
})
```

## จัดการ failure ให้ถูก

KafkaJS จะ throw ถ้า transactional operation ล้มเหลว และ error ที่ throw ออกมาจะพก flag **`retriable`** ที่ตัดสินวิธีกู้คืน เคสปกติคือ **retriable**: `txn.abort()` แล้วปล่อยให้ batch รันใหม่จาก offset ที่ commit ล่าสุด ส่วน error แบบ **non-retriable** (`e.retriable === false`) แปลว่ามี producer ตัวใหม่เข้ามายึด `transactionalId` ของคุณไปแล้ว คุณไปต่อไม่ได้ ให้ `disconnect()` instance นี้แล้วปล่อยให้ตัวที่ยังดีทำงานแทน

```typescript
} catch (e) {
  await txn.abort()                  // roll back and retry the batch
  if (!e.retriable) {                // a zombie was fenced — this producer cannot recover
    await producer.disconnect()      // give up this instance
    throw e
  }
}
```

<Mermaid title="consume-transform-produce เป็นหน่วย atomic เดียว" code={eosDiagram} />

export const eosDiagram = `flowchart LR
  in["consume from orders"] --> begin["producer.transaction()"]
  begin --> transform["transform + txn.send to orders-enriched"]
  transform --> off["txn.sendOffsets(input offsets)"]
  off --> commit["txn.commit(): outputs + offsets atomic"]
  commit --> in`;

<Callout title="สรุปหนึ่งบรรทัด">
exactly-once **consume-transform-produce** ทำงานด้วยการเอา input offset ของ consumer **เข้าไปอยู่ใน transaction ของ producer** ผ่าน `txn.sendOffsets()` เพื่อให้ output record กับความคืบหน้าของ input commit แบบ atomic ตอน fail ให้ `txn.abort()` แล้ว retry ถ้า error ที่ throw เป็นแบบ **non-retriable** (`e.retriable === false` มี zombie ถูก fence) ให้ `disconnect()` แล้วปล่อยให้อีก instance ทำงานแทน นี่ให้ exactly-once สำหรับ pipeline ที่อยู่ภายใน Kafka
</Callout>

export const quizEos = [
  {
    q: 'ใน consume-transform-produce ทำไมต้อง commit input offset ภายใน transaction',
    options: [
      'เพื่อให้เร็วขึ้น',
      'เพื่อให้ output record กับความคืบหน้าของ input commit หรือ abort พร้อมกัน ไม่งั้นการล่มจะทำข้อมูลซ้ำหรือหาย',
      'เพราะ commit offset วิธีอื่นไม่ได้',
      'เพื่อลดจำนวน partition',
    ],
    answer: 1,
  },
  {
    q: 'การเรียกไหน commit consumer offset เป็นส่วนหนึ่งของ producer transaction',
    options: [
      'consumer.commitOffsets()',
      'txn.sendOffsets({ consumerGroupId, topics }) บน transaction',
      'producer.flush()',
      'consumer.seek()',
    ],
    answer: 1,
  },
  {
    q: 'ควรจัดการ error แบบ non-retriable (`e.retriable === false`) ยังไง',
    options: [
      'retry transaction ทันที',
      'มองว่ากู้คืนไม่ได้ — abort, disconnect producer แล้วปล่อยให้อีก instance ทำงานแทน (มี zombie ถูก fence)',
      'เพิกเฉย',
      'สลับไปใช้ auto-commit',
    ],
    answer: 1,
  },
  {
    q: 'การตอบสนองที่ถูกต้องต่อ error แบบ retriable ภายใน transaction คืออะไร',
    options: [
      'commit ต่อไปเลย',
      'abort transaction แล้ว retry batch จาก offset ที่ commit ล่าสุด',
      'ลบ topic ทิ้ง',
      'ปิด idempotence',
    ],
    answer: 1,
  },
];

<Quiz client:visible id="delivery-and-transactions/exactly-once-processing" questions={quizEos} />

<ProgressTracker client:visible id="delivery-and-transactions/exactly-once-processing" />
