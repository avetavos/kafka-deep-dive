---
title: 'Offsets & Commits'
description: 'consumer บันทึกความคืบหน้าอย่างไร ทั้ง committed offset, auto-commit เทียบกับ manual commitOffsets ใน KafkaJS และดีฟอลต์แบบ at-least-once'
sidebar:
  order: 3
---

import Mermaid from '../../../../components/Mermaid.astro';
import Callout from '../../../../components/Callout.astro';
import Quiz from '../../../../components/Quiz.tsx';
import ProgressTracker from '../../../../components/ProgressTracker.tsx';

## แนวคิดในหนึ่งประโยค

consumer บันทึกว่าประมวลผลไปถึงไหนด้วยการ **commit offset** ต่อ partition Kafka เก็บมันไว้ใน topic ภายในชื่อ `__consumer_offsets` และ **จุดที่คุณ commit** เทียบกับ **จุดที่คุณ process** เป็นตัวตัดสินว่าคุณจะทำ record หายหรือซ้ำได้

## committed เทียบกับ current position

แต่ละ partition พก offset สองตัวมาด้วย:

- **Position** offset ตัวถัดไปที่ consumer จะ fetch (ขยับไปเรื่อยเมื่อ consume)
- **Committed offset** offset ตัวสุดท้ายที่ consumer บันทึกอย่างถาวรว่า "เสร็จแล้ว"

เมื่อ restart หรือ rebalance consumer จะกลับมาเริ่มจาก **committed** offset ดังนั้น commit จึงเป็นคำสัญญาว่า "ทุกอย่างก่อนหน้านี้ process แล้ว อย่าส่งมาอีก"

## auto-commit สะดวกแต่เสี่ยง

โดยดีฟอลต์ KafkaJS จะ commit ให้อัตโนมัติ (`autoCommit: true`): มัน commit offset ที่ส่งไปแล้วเป็นระยะ ทุก ๆ `autoCommitInterval` มิลลิวินาที (หรือหลังครบ `autoCommitThreshold` message) มันง่าย แต่มัน commit ตามตารางเวลาโดยไม่สนว่าการประมวลผลของคุณสำเร็จหรือไม่ ถ้า crash หลัง auto-commit แต่ก่อน process จะทำให้ record เหล่านั้น **หาย** สำหรับงานที่สำคัญ ให้ตั้ง `autoCommit: false` แล้ว commit **หลัง** process:

```typescript
const consumer = kafka.consumer({ groupId: 'order-processor' });
await consumer.connect();
await consumer.subscribe({ topic: 'orders' });

await consumer.run({
  autoCommit: false,                                    // take control of commits
  eachMessage: async ({ topic, partition, message }) => {
    await process(message);                             // do the work FIRST
    // then commit the NEXT offset to read (offset + 1) — at-least-once
    await consumer.commitOffsets([
      { topic, partition, offset: (Number(message.offset) + 1).toString() },
    ]);
  },
});
```

## manual commit ใน KafkaJS

เมื่อ `autoCommit: false` คุณ commit เองด้วย **`consumer.commitOffsets([...])`** ซึ่งคืน Promise **await มัน** เพื่อให้แน่ใจว่า commit ถูก ack ก่อนไปต่อ — ตัวเลือกที่ปลอดภัยและช้ากว่านิดหน่อย เหมือน commit แบบ synchronous ถ้าไม่ `await` ก็ยิงมันเบื้องหลังเพื่อ throughput โดยยอมรับว่า error จะไม่ถูกจับตรงนั้น ไม่ว่าทางไหนคุณ commit offset **ตัวถัดไป** ที่ต้องอ่าน (`offset + 1`) เพราะนั่นคือจุดที่ consumer ควรกลับมาเริ่ม

## จะเริ่มอ่านจากไหน และ replay ด้วย seek()

committed offset จะมีประโยชน์ก็ต่อเมื่อมันมีอยู่ ครั้งแรกที่ group อ่าน partition หนึ่ง หรือถ้า committed offset หมดอายุตาม retention ไปแล้ว flag `fromBeginning` บน `subscribe()` ของ KafkaJS จะเป็นตัวตัดสินว่าจะเริ่มตรงไหน:

- **`fromBeginning: true`** เริ่มจากต้น partition แล้ว process history ที่เก็บไว้ทั้งหมดใหม่ (policy `earliest`)
- **`fromBeginning: false`** เริ่มจากท้าย อ่านเฉพาะ record ที่เข้ามานับจากนี้ (ดีฟอลต์ `latest`)

คุณยัง override ตำแหน่งเองได้ด้วย `consumer.seek()` ย้าย partition ไปที่ offset ไหนก็ได้เพื่อ **replay** record เก่าหรือข้ามไปข้างหน้า:

```typescript
// where to begin when there is no committed offset:
// fromBeginning: true = earliest (replay all history), false = latest (default)
await consumer.subscribe({ topic: 'orders', fromBeginning: true });

// Replay partition 0 of "orders" from offset 100 (call after run() has started)
consumer.seek({ topic: 'orders', partition: 0, offset: '100' }); // next fetch starts at offset 100
// use offset '0' to replay the whole partition from the start
```

<Mermaid title="Commit หลัง process ให้ผลเป็น at-least-once" code={commitDiagram} />

export const commitDiagram = `flowchart LR
  poll["poll batch"] --> proc["process records"]
  proc --> commit["commitOffsets() after success"]
  commit --> poll
  proc -.->|crash before commit| replay["restart re-reads from last commit (duplicates)"]`;

## ทำไมดีฟอลต์นี้จึงเป็น at-least-once

commit **หลัง** process แล้ว crash ระหว่างสองจุดนั้น หมายความว่า record เหล่านั้นจะถูกอ่านและ process ซ้ำตอน restart คือ **at-least-once** อาจซ้ำได้ ส่วน commit **ก่อน** process แล้ว crash หมายถึงมันถูกข้ามไป คือ **at-most-once** อาจหายได้ at-least-once เป็นดีฟอลต์ที่ใช้กันทั่วไป จะทำให้มันเป็น exactly-once จริง ๆ ต้องอาศัย idempotent processing หรือ transaction ซึ่ง module ถัดไปจะครอบคลุม

<Callout title="สรุปหนึ่งบรรทัด">
consumer commit **offset** เพื่อทำเครื่องหมายความคืบหน้า (เก็บใน `__consumer_offsets`) และกลับมาเริ่มจาก **committed** offset หลัง restart **auto-commit** (`autoCommit: true`) ง่ายแต่ commit ตามตารางเวลาและอาจทำ record หาย เพื่อความถูกต้องให้ตั้ง **`autoCommit: false`** แล้ว **commit หลัง process** ด้วย `consumer.commitOffsets([...])` การ commit หลัง process ให้ผลเป็น **at-least-once** (อาจซ้ำ) คือดีฟอลต์ที่ต้องออกแบบเผื่อไว้ ใช้ **`fromBeginning`** ตัดสินว่า group ใหม่เอี่ยมจะเริ่มตรงไหน (earliest/latest) และใช้ **`seek()`** เพื่อ replay
</Callout>

export const quizOffsets = [
  {
    q: 'Kafka เก็บ committed consumer offset ไว้ที่ไหน?',
    options: [
      'บน local disk ของ consumer เท่านั้น',
      'ใน topic ภายในชื่อ `__consumer_offsets`',
      'ใน ZooKeeper',
      'ในฐานข้อมูล relational ที่คุณต้องจัดหาเอง',
    ],
    answer: 1,
  },
  {
    q: 'ทำไม auto-commit จึงเสี่ยงสำหรับงานประมวลผลที่สำคัญ?',
    options: [
      'มัน commit น้อยเกินไปจนไม่มีประโยชน์',
      'มัน commit ตามตารางเวลาโดยไม่สนว่า process สำเร็จหรือไม่ ดังนั้น crash อาจทำ record หาย',
      'มันทำ record ซ้ำทุกตัว',
      'มันปิดการทำงานของ consume loop',
    ],
    answer: 1,
  },
  {
    q: 'การ commit offset หลัง process ให้ delivery semantic แบบใด?',
    options: [
      'At-most-once (record อาจหาย)',
      'At-least-once — crash ก่อน commit ทำให้ record ถูก process ซ้ำ (อาจซ้ำได้)',
      'Exactly-once โดยไม่ต้องทำอะไรเพิ่ม',
      'ไม่มี delivery guarantee เลย',
    ],
    answer: 1,
  },
  {
    q: 'เมื่อ manual commit ใน KafkaJS การ await consumer.commitOffsets(...) ให้อะไรกับคุณ?',
    options: [
      'ไม่ให้อะไร commit เกิดขึ้นทันทีและไม่ถูก ack เสมอ',
      'มันรอจน commit ถูก ack ก่อนไปต่อ — ตัวเลือกที่ปลอดภัย เทียบกับการยิงเบื้องหลังเพื่อ throughput',
      'มันลบ offset เก่าออกจาก topic',
      'มันสร้าง consumer group ใหม่',
    ],
    answer: 1,
  },
];

<Quiz client:visible id="consumers/offsets-and-commits" questions={quizOffsets} />

<ProgressTracker client:visible id="consumers/offsets-and-commits" />
