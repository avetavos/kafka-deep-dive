---
title: 'Transactions'
description: 'Atomic multi-partition writes with the transactional producer — transactionalId, the transaction lifecycle, read_committed, and the KIP-890 hardening in Kafka 4.x.'
sidebar:
  order: 2
---

import Mermaid from '../../../../components/Mermaid.astro';
import Callout from '../../../../components/Callout.astro';
import Quiz from '../../../../components/Quiz.tsx';
import ProgressTracker from '../../../../components/ProgressTracker.tsx';

## The idea in one sentence

A Kafka **transaction** lets a producer write to multiple partitions (and commit consumer offsets) **atomically** — everything in the transaction becomes visible together on commit, or none of it does on abort.

## Configuring a transactional producer

Transactions build on the idempotent producer. You add a **`transactionalId`** — a stable identifier that lets Kafka fence out zombie instances of the same logical producer — along with `idempotent: true` and `maxInFlightRequests: 1`. Connecting the producer registers the transactional id; unlike the Java client, KafkaJS has no separate `initTransactions()` call.

```typescript
import { Kafka } from 'kafkajs'

const kafka = new Kafka({ clientId: 'payments', brokers: ['localhost:9092'] })

// A transactional producer: a stable transactionalId is the key ingredient
const producer = kafka.producer({
  transactionalId: 'payments-tx-1',   // stable id enables transactions + zombie fencing
  idempotent: true,                    // required foundation
  maxInFlightRequests: 1,
})

await producer.connect()               // connecting registers the transactional id
```

## The transaction lifecycle

Start a unit of work with `producer.transaction()`. Any sends on the returned `txn` are buffered into the transaction; `txn.commit()` makes them all visible atomically, and `txn.abort()` discards them:

```typescript
const txn = await producer.transaction()   // begin
try {
  await txn.send({ topic: 'accounts', messages: [{ key: debitKey, value: debit }] })
  await txn.send({ topic: 'accounts', messages: [{ key: creditKey, value: credit }] })
  await txn.commit()     // both writes appear together, or neither
} catch (e) {
  await txn.abort()      // roll back the whole unit
}
```

## Consumers must read committed

Transactions only matter if readers ignore aborted and in-flight data. Set the consumer to **`readUncommitted: false`** — **read_committed**, and the KafkaJS default — so it never sees records from aborted or open transactions:

```typescript
const consumer = kafka.consumer({
  groupId: 'ledger',
  readUncommitted: false,   // read_committed: skip aborted and in-flight records
})
```

<Mermaid title="A transaction commits multiple writes atomically" code={txnDiagram} />

export const txnDiagram = `flowchart LR
  init["connect() once (registers txn id)"] --> begin["producer.transaction()"]
  begin --> s1["txn.send to accounts-0"]
  begin --> s2["txn.send to accounts-2"]
  s1 --> commit["txn.commit(): all visible together"]
  s2 --> commit
  begin -.->|on error| abort["txn.abort(): nothing visible"]`;

## KIP-890 in Kafka 4.x

Kafka 4.x finalizes **KIP-890, Transactions Server-Side Defense**, which strengthens the transactional protocol against edge cases like hanging transactions and zombie producers. When the 4.0 upgrade is finalized the new, hardened protocol is enabled — so transactional pipelines on Kafka 4.x are more robust than on older versions with no code change on your side.

<Callout title="The one-line takeaway">
A **transaction** makes multi-partition writes **atomic**: configure `idempotent: true` plus a stable **`transactionalId`** (and `maxInFlightRequests: 1`), then `producer.transaction()` → `txn.commit()` (or `txn.abort()`). Readers must use **`readUncommitted: false`** (read_committed) to skip aborted/open data. Kafka 4.x hardens all of this via **KIP-890**, transparently.
</Callout>

export const quizTransactions = [
  {
    q: 'What does a Kafka transaction guarantee?',
    options: [
      'Faster writes to a single partition',
      'Multiple writes (and offset commits) become visible atomically — all on commit, or none on abort',
      'Automatic compression',
      'That records are never retained',
    ],
    answer: 1,
  },
  {
    q: 'What is the role of `transactionalId`?',
    options: [
      'It names the topic',
      'A stable identifier for the logical producer that lets Kafka fence out zombie instances',
      'It sets the partition count',
      'It is the consumer group id',
    ],
    answer: 1,
  },
  {
    q: 'What must a consumer set to correctly read transactional data?',
    options: [
      '`readUncommitted: true`',
      '`readUncommitted: false` (read_committed), so it skips aborted and in-flight records',
      '`autoCommit: true`',
      'A larger fetch size',
    ],
    answer: 1,
  },
  {
    q: 'What does KIP-890 do in Kafka 4.x?',
    options: [
      'Removes transactions',
      'Strengthens the transactional protocol (server-side defense) against hanging transactions and zombies, transparently',
      'Requires ZooKeeper for transactions',
      'Disables idempotence',
    ],
    answer: 1,
  },
];

<Quiz client:visible id="delivery-and-transactions/transactions" questions={quizTransactions} />

<ProgressTracker client:visible id="delivery-and-transactions/transactions" />
