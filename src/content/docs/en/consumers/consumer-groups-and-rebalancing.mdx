---
title: 'Consumer Groups & Rebalancing'
description: 'How consumer groups share partitions, what a rebalance is, and the new KIP-848 consumer rebalance protocol that is generally available in Kafka 4.x.'
sidebar:
  order: 2
---

import Mermaid from '../../../../components/Mermaid.astro';
import Callout from '../../../../components/Callout.astro';
import Quiz from '../../../../components/Quiz.tsx';
import ProgressTracker from '../../../../components/ProgressTracker.tsx';

## The idea in one sentence

A **consumer group** is a set of consumers that share a topic's partitions between them — each partition is handled by exactly one consumer in the group — and **rebalancing** is the process of reassigning partitions when membership changes.

## Groups divide the work

Every consumer declares a `group.id`. Kafka gives each **partition** to exactly **one** consumer within a group, so a topic with 6 partitions can be processed by up to 6 consumers in parallel. Add a 7th consumer and it sits idle — partitions, not consumers, are the ceiling on parallelism.

Different groups are independent: `analytics` and `billing` each get their own full copy of the stream, tracking their own offsets. Within one group, the partitions are divided.

You can inspect a group, its members, and its lag from the CLI:

```bash
# Show members, assigned partitions, and lag for a group
kafka-consumer-groups.sh --bootstrap-server localhost:9092 \
  --describe --group billing
```

<Mermaid title="One group divides partitions; another group reads the same topic independently" code={groupDiagram} />

export const groupDiagram = `flowchart TB
  subgraph t["Topic orders: 3 partitions"]
    p0["orders-0"]
    p1["orders-1"]
    p2["orders-2"]
  end
  subgraph g1["Group billing"]
    c1["consumer A"]
    c2["consumer B"]
  end
  p0 --> c1
  p1 --> c1
  p2 --> c2
  t --> g2["Group analytics (own offsets, full copy)"]`;

## Rebalancing

When a consumer joins, leaves, or dies — or partitions are added — the group must **rebalance**: redistribute partitions among the current members. The classic protocol did this with a **stop-the-world** step: every consumer stopped processing, gave up its partitions, and waited for a new assignment. On large groups that pause hurt.

## KIP-848: the new rebalance protocol

Kafka 4.0 ships the **next-generation consumer rebalance protocol (KIP-848)** as **generally available**. It moves assignment logic to the broker-side **group coordinator** and makes rebalances **incremental** — only the partitions that actually move are revoked, and the rest keep processing. The result is shorter, less disruptive rebalances, especially for large groups.

```properties
# Opt into the new consumer group protocol (KIP-848), GA in Kafka 4.0
group.protocol=consumer
# The classic (older) protocol is still available:
# group.protocol=classic
```

<Callout title="If you learned Kafka a few years ago">
The old advice was "avoid rebalances, they stop the world." With KIP-848 (`group.protocol=consumer`, GA in Kafka 4.0) rebalances are **incremental and broker-driven** — untouched partitions keep flowing. Once activated, a group can only be downgraded to Kafka 3.4.1 or newer.
</Callout>

<Callout title="The one-line takeaway">
A **consumer group** splits a topic\'s partitions among its members — one partition per consumer, so partition count caps parallelism — while separate groups each read the whole stream. Membership changes trigger a **rebalance**; Kafka 4.0 makes this far smoother with the **KIP-848** protocol (`group.protocol=consumer`), which is broker-driven and **incremental** instead of stop-the-world.
</Callout>

export const quizGroups = [
  {
    q: 'Within a single consumer group, how many consumers read a given partition?',
    options: [
      'All consumers read every partition',
      'Exactly one — each partition is assigned to a single consumer in the group',
      'At least two, for redundancy',
      'None; partitions are read by the broker',
    ],
    answer: 1,
  },
  {
    q: 'You have a topic with 4 partitions and start 6 consumers in one group. What happens?',
    options: [
      'All 6 consumers share every partition',
      '4 consumers get one partition each; the other 2 sit idle — partitions cap parallelism',
      'Kafka creates 2 more partitions automatically',
      'The group fails to start',
    ],
    answer: 1,
  },
  {
    q: 'What improved about rebalancing in Kafka 4.0 with KIP-848?',
    options: [
      'Rebalances were removed entirely',
      'Rebalances became incremental and broker-driven — only moved partitions are revoked, instead of stop-the-world',
      'Consumers now share partitions',
      'Offsets are no longer tracked',
    ],
    answer: 1,
  },
  {
    q: 'How do two different consumer groups reading the same topic relate?',
    options: [
      'They share offsets and compete for records',
      'They are independent — each gets a full copy of the stream and tracks its own offsets',
      'Only one group can read a topic at a time',
      'The second group must wait for the first to finish',
    ],
    answer: 1,
  },
];

<Quiz client:visible id="consumers/consumer-groups-and-rebalancing" questions={quizGroups} />

<ProgressTracker client:visible id="consumers/consumer-groups-and-rebalancing" />
